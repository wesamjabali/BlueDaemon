const config = require("../config.json");
module.exports = {
  name: "",
  description: "",
  facultyOnly: false,
  privileged: false,
  usage: config.prefix + "",
  execute: async (msg, isModerator, isFaculty, client) => {},
};
